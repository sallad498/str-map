# str-map

Interactive webpage project scaffold.

## Structure

- index.html
- src/css/
- src/js/
- assets/

## Start

Open index.html in a browser or use a simple HTTP server (e.g., `npx serve` or `python -m http.server`).
